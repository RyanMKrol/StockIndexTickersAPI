{
  "author": "Ryan Krol",
  "name": "StockIndexTickersApi",
  "description": "An API to fetch the tickers currently in the FTSE 100/250/350/SmallCap",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel --presets es2015 -d lib/ src",
    "start": "npm run-script build && node lib/app.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.3",
    "curl": "^0.1.4",
    "express": "^4.17.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
